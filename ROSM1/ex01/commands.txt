1) > filename
   Создание нового файла в терминале
   
2) whereis programmname
   Возвращает полный путь к программе pragramname
   
3) gzip filename
   Создание архива
   
   gunzip filename.gz
   Распаковка архива
   
4) dpkg -i package.deb
   Установка пакета .deb

5) cat fiename.txt
   Отображает содержимое файла в окне терминала

6) df
   Отображает объем диска и количество свободного места на нем
 
7) pwd
   Показывает директорию в которой вы находитесь

8) whatis programname
   Выводит краткое описание программы
   
9) lsblk
   Выводит информацию о дисках, разделах и их названиях

10) wget link
   Загрузка файлов из сети
